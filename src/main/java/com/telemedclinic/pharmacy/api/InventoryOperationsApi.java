package com.telemedclinic.pharmacy.api;

public interface InventoryOperationsApi {
    // API khusus untuk apoteker dalam mengelola stok
    // void addToCatalog(Long pharmacyId, Long medicineId, double price, int initialStock);
    // void updateStock(Long inventoryItemId, int newStock);
}
